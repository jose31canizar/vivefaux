var React = require('react');

var BaselineFooter = React.createClass({
    render: function () {
        return (
            <div className={'BaselineFooter'}>
                <p>powered by Vivo Applications</p>
            </div>
        );
    }
});

module.exports = BaselineFooter;
